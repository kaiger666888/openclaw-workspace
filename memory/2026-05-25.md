# 2026-05-25 Daily Notes

## kais-elon Skill 创建 (09:50-10:27)

### 完成
- 创建 kais-elon skill（Elon Musk 思维与做事方法论教练）
- 评分 97/100 🏆，验证全通过
- 已推送到 GitHub: https://github.com/kaiger666888/kais-elon

### 核心内容
- 五步算法：质疑需求 → 删到痛 → 简化 → 加速 → 自动化
- 六大思维模型：第一性原理、10x思维、反馈循环密度、极致工作伦理、跨域迁移、白痴指数
- 诊断模式 + 训练模式

### 决策
- **架构设计和工程设计时必须借鉴 kais-elon 的思想**（Kai 明确要求）
- kais-mentor 不再需要，kais-elon 作为唯一的思维指导 skill
# 2026-05-25 Daily Notes

## kais-elon Skill 创建 (09:50-10:27)

### 完成
- 创建 kais-elon skill（Elon Musk 思维与做事方法论教练）
- 评分 97/100 🏆，验证全通过
- 已推送到 GitHub: https://github.com/kaiger666888/kais-elon

### 核心内容
- 五步算法：质疑需求 → 删到痛 → 简化 → 加速 → 自动化
- 六大思维模型：第一性原理、10x思维、反馈循环密度、极致工作伦理、跨域迁移、白痴指数
- 诊断模式 + 训练模式

### 决策
- **架构设计和工程设计时必须借鉴 kais-elon 的思想**（Kai 明确要求）
- kais-mentor 不再需要，kais-elon 作为唯一的思维指导 skill

---

## kais-aigc-platform 全链路联调 (13:00-16:30)

### 目标
修复 movie-agent Pipeline 全链路：项目创建 → 剧本 → AI 事件 → Pipeline 8 phase → 真实图片渲染(RTX 3090) → TTS → Review → Delivery

### 已完成
- [x] **Review 平台时区修复** — `DateTime(timezone=True)` + Alembic 迁移 (commit `57d4a0c` kais-review-platform)
- [x] **ComfyUI 主机修复** — `comfyui` → `172.17.0.1` (commit `c747d0a` kais-aigc-platform)
- [x] **LFS 清理** — kais-gold-team 移除 10 PNG (commit `6446715`)
- [x] **TTS 引擎** — `tts-local` 引擎, edge-tts 子进程, 测试通过 22KB (commit `428de29` kais-gold-team)
- [x] **云端引擎** — 真实 API classes: cloud_jimeng/cloud_kling/cloud_seedance (commit `ab47de6` kais-gold-team)
- [x] **Pipeline 执行器** — 0/8 → 8/8 completed (commit `63f44d3` kais-aigc-platform)
- [x] **Novel data unwrap 修复** — core-backend API 返回 `{data:{data:[...]}}` 嵌套，加了双层 unwrap
- [x] **Event 解析修复** — pipe 分隔表格只有数据行没有 header，`rows.slice(1)` 跳过了唯一行
- [x] **gold-team-client API 路径修复** — `/api/tasks` → `/api/v1/tasks`
- [x] **gold-team-client priority 修复** — 数字 `5` → 字符串 `"normal"`
- [x] **TTS task_type 修复** — `tts_generation` → `tts`
- [x] **Pipeline 从 core-backend 拉数据** — 项目配置、小说内容、事件解析全通
- [x] **TTS 真实渲染** — 3 条旁白 × edge-tts = 3 个 voice.wav (33-36KB)
- [x] **8/8 phases completed**
- [x] **ComfyUI 依赖修复** — comfy_aimdo, comfy_kitchen, av, blake3, torchsde, torchaudio==2.6.0

### 待修
- [ ] **图片渲染保存失败** — scene prompt 生成了，gold-team 任务提交了，但图片没保存到 assets/（waitForTask 返回值处理）
- [ ] **Review 401** — review-platform `/api/v1/reviews` 需要 API key 认证
- [ ] **云引擎 API keys** — JIMENG_SESSION_ID, KLING_ACCESS_KEY+SECRET_KEY 未配置
- [ ] **GPU passthrough** — gold-team 容器内无 GPU（nvidia-container-toolkit）
- [ ] **前端 UI 验证**
- [ ] **CI/CD pipeline**

### 关键 Bug 修复记录
1. **Novel data unwrap**: `novelJson.data` 返回 `{data:[...], total:1}` 不是数组，走了 else 分支包成 `[{data:[...]}]`
2. **Event rows.slice(1)**: pipe 分隔表格只有 1 行数据，`slice(1)` 跳过了唯一行
3. **gold-team API path**: Docker 网络内用 `gold-team:8002` 但 GoldTeamClient 硬编码了旧路径 `/api/tasks`
4. **priority field**: 传了数字 `5`，gold-team V6 期望字符串 `"normal"/"high"/"critical"`
5. **TTS task_type**: 传了 `tts_generation`，gold-team 期望 `tts`

### Git Commits
- `kais-aigc-platform`: `c747d0a` (compose fix), `63f44d3` (pipeline executor), `9c91b6a` (full integration)
- `kais-gold-team`: `6446715` (LFS cleanup), `428de29` (TTS), `ab47de6` (cloud engines), `88df590` (cloud engine fix)
- `kais-review-platform`: `57d4a0c` (timezone fix)

### 服务状态
- core-backend: localhost:8000 (SQLite)
- movie-agent: localhost:8001
- gold-team: localhost:8002 (5 engines: comfyui-local, mock, tts-local, cloud-jimeng, cloud-kling, cloud-seedance)
- review-platform: localhost:8091
- ComfyUI: 172.17.0.1:8188 (RTX 3090 24GB)
- postgres: port 5490, redis: port 6390

### 测试数据
- Project ID: 1779688174552 (赛博朋克-真实测试, cyberpunk, 16:9, flux-dev, wan2.2-14b)
- Pipeline ID: pipe_1779696238234_x6y8hl (latest, 8/8 completed)
- TTS output: 3 files in gold-team output dir (ma-1779696240*)

### 决策
- **TTS 方案**: edge-tts 子进程（CosyVoice 未安装，自动降级）
- **Quality-gate**: MVP 期间低评分警告继续，不崩溃
- **云引擎**: 有真实代码但 API keys 不存在时自动降级到 mock
- **子任务策略**: 3 次子任务全部超时/失败（gateway restart），改为直接在主会话修代码

### 教训
- **子任务不适合长时间 Docker 操作**: exec + docker build + docker compose 每步都要等，容易超时
- **数据 unwrap 要考虑多层嵌套**: API 返回格式 `{data:{data:[...]}}` 需要 2 层 unwrap
- **表格解析没有 header 行时要处理**: pipe 分隔表格可能只有数据行

## 前端联调 (21:00-22:00)

### 完成
1. **core-backend 三组代理路由**（Express v5 middleware 模式）
   - `/api/proxy/movieAgent/*` → movie-agent:8001（health/pipeline/tasks）
   - `/api/proxy/reviewPlatform/*` → review-platform:8090（shot-cards API）
   - `/api/proxy/goldTeam/*` → gold-team:8002（文件下载/task status）
   - 文件：`src/routes/proxy/movieAgent.ts`, `reviewPlatform.ts`, `goldTeam.ts`
   - 关键修复：Express v5 不支持 `/*` 通配符，改用 `router.use()` 中间件模式
   - 关键修复：fetch URL 需要拼接 query string（`req.query`）
   
2. **Toonflow 前端配置**
   - 访问地址：http://192.168.71.166:8000
   - 登录：admin / admin123
   - baseUrl 配为：http://192.168.71.166:8000/api
   - 前端 API 调用验证通过（getProject/getModelList/login）

3. **E2E Pipeline 测试**（通过代理）
   - pipeline create/start/status 全部通过代理验证
   - 8/8 phases completed
   - 产出物：场景图 1.8MB PNG + 2条 TTS 音频

### 发现的问题
- 通过 API 直接创建的 pipeline，内部 stages（scenario/voice/storyboard/scene）duration=0
- 原因：pipeline 从 core-backend `/api/novel/getNovel` 拉取小说数据，需要在 Toonflow UI 先创建项目并上传小说
- 这是设计意图：前端 → 创建项目 → 上传小说 → 发起 pipeline

### 状态
- 所有 5 个容器 healthy（core-backend/movie-agent/gold-team/review-platform/postgres/redis）
- 代理、前端、API 全通
- 下一步：在 Toonflow UI 创建项目 → 上传小说 → 发起完整 E2E pipeline
