# 2026-04-15 Memory Log

## 即梦登录流程验证与 skill 更新 (20:30-09:02)

### 关键发现：即梦官网登录流程
1. 打开首页 `https://jimeng.jianying.com/ai-tool/home?type=image`
2. 点击「图片生成」卡片
3. 弹出协议弹窗 → 点击「同意」按钮（`.lv-modal-content button` filter `hasText:'同意'` last()）
4. **自动弹出 OAuth 二维码**（不需要手动点登录按钮，点同意后直接弹出）
5. 新 tab 打开 `open.douyin.com/platform/oauth/pc/auth` 显示扫码页
6. 轮询检测登录完成 → 提取 cookies

### 注意事项
- 弹窗 class 名每次不同（CSS modules hash），必须用 text filter 而非精确 class
- 同意按钮选择器：`page.locator('.lv-modal-content button').filter({hasText:'同意'}).last()`
- **不要用 JS evaluate 点击**，用 Playwright locator 原生点击才能正确触发事件
- 用 `launchPersistentContext` 保存登录态到 `.browser-data/` 目录
- 浏览器进程不能退出，需要 background 运行保持住等用户扫码

### Session 提取结果
- **sessionid**: `ff2d60414e15fdcf25978c23bdb7f311`
- persistent context 保存在 `skills/kais-jimeng/.browser-data/`
- 登录态跨进程持久化有效（关闭再打开仍保持登录）

### Skill 更新
- 更新了 `kais-jimeng/lib/session-manager.js` 的 `loginViaBrowser` 函数
- 移除了旧版触发器（`text=一键配置` 等），改用验证过的流程
- 移除了手动点登录按钮步骤（点同意后自动弹出）
- 清理了 16 万个浏览器缓存文件（`.browser-data/` 未 gitignore 导致）
- 已 push 到 GitHub

### kais-jimeng skill 位置
- `/home/kai/.openclaw/workspace/skills/kais-jimeng/`
- 关键文件: `lib/session-manager.js`, `.browser-data/`, `.env`, `.jimeng-cookies.json`
